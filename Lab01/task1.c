/*
#include <stdio.h>
#include <math.h>
#include <locale.h>

int main(int argc, char *argv) {
	setlocale(LC_CTYPE, "rus");

	float x = 5;
	float y = 8.2537;
	float f = (sin(x) + cos(2*y - x)) / (cos(x) - sin(2*y - x));

	printf("x = %.4f\n", x);
	printf("y = %.4f\n", y);
	printf("f(x,y) = %.4f\n", f);

	printf("¬ведите x: ");
	scanf_s("%f", &x);
	printf("¬ведите y: ");
	scanf_s("%f", &y);

	f = (sin(x) + cos(2 * y - x)) / (cos(x) - sin(2 * y - x));

	printf("x = %.4f\n", x);
	printf("y = %.4f\n", y);
	printf("f(x,y) = %.4f\n", f);

	system("pause");
	return 0;
} */